<head>
<meta name="viewport" content="width=device-width, initial-scale=1">
<style>
* {
  box-sizing: border-box;
}

body {
  font-family: Arial, Helvetica, sans-serif;
}

/* Float four columns side by side */
.column {
  float: left;
  width: 25%;
  padding: 0 10px;
}

/* Remove extra left and right margins, due to padding */
.row {margin: 0 -5px;}

/* Clear floats after the columns */
.row:after {
  content: "";
  display: table;
  clear: both;
}

/* Responsive columns */
@media screen and (max-width: 600px) {
  .column {
    width: 100%;
    display: block;
    margin-bottom: 20px;
  }
}

/* Style the counter cards */
.card {
  box-shadow: 0 4px 8px 0 rgba(0, 0, 0, 0.2);
  padding: 16px;
  text-align: center;
  background-color: #f1f1f1;
}
</style>
</head>
      <h3><%= @supplier.name %></h3>
      <p>supplier Details</p>

  <hr>
  <h3> Product details <%= check_box_tag 'inactive_products', @supplier.id, @inactive, :class => "inactive-check" %></h3>
<% @products.each do |product| %>
  <div class="column">
    <div class="card">
      <h3><%=product.id %><%=product.title %></h3>
      <p>product Details</p>
    </div>
  </div>
</div>
<% end %>

<script type="text/javascript">
  
 $('#inactive_products').on('change',function() {
      if(this.checked){
        this.checked = true
        window.location.href = "http://localhost:3000/suppliers/<%= @supplier.id%>?inactive=true"
      }else{
        this.checked = false
        window.location.href = "http://localhost:3000/suppliers/<%= @supplier.id%>?inactive=false"
      }
 });
</script>>

